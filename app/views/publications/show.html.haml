.container
  .jumbotron.m-4.mt-2
    %h1= @publication.title
    %h6= @publication.description
    -if can_edit?(@publication)
      =link_to 'Edit', edit_publication_path(@publication),  class:"btn btn-dark ml-2"
      =link_to 'Destroy', publication_path(@publication.id), method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-dark ml-2"


  \|
  = link_to 'Back', publications_path
